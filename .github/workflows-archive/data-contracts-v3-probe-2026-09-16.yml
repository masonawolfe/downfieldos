name: Data - Contracts Test v3

on:
  schedule:
    - cron: '0 12 1 * *'
  workflow_dispatch:
    inputs:
      season:
        description: 'Contract-year season (deals ending this year → contract_year)'
        required: false
        default: '2026'

permissions:
  contents: write

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
