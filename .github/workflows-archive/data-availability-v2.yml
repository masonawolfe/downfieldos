# E-015 (2026-09-16): dead file, kept as a marker. The real availability
# workflow was moved to data-availability.yml under fresh registration; this
# file is the failed rename experiment that never parsed on GitHub's side.
# The `on:` block below is manual-only + no-op so it does not add noise to
# the Actions tab.
name: (dead) data-availability-v2

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled — see .github/workflows/data-availability.yml"
