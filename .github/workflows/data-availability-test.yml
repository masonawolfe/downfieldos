# E-015 (2026-09-16): dead file, kept as a marker. Content promoted to
# data-availability.yml under fresh registration.
name: (dead) data-availability-test

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled — see .github/workflows/data-availability.yml"
